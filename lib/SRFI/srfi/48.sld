;;; SRFI 48: Intermediate Format Strings.
;;;
;;; $Id$

(define-library (srfi 48 intermediate-format-strings)

  (export format)

  (import (srfi :48 intermediate-format-strings)))


(define-library (srfi 48)

  (export format)

  (import (srfi 48 intermediate-format-strings)))

;;; eof
