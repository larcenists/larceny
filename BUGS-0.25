Known bugs in Larceny 0.25
--------------------------

- Discovered by arithmetic regression test ("test 6"):
     (inexact->exact 0.0)
  bombs with
     Error: quotient:  division by zero: 8 0
  Happens in both generational and stop+copy systems.

- Heap statistics in sclarceny may not be right due to a hasty patch
  to get the system running (used_estack() always returns 0).
